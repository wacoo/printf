#include "main.h"

/**
 * print_chr - prints characters
 * @count: count
 * @ar: argument
 *
 * Return: count of printed chars
 */
int print_chr(char ar, int count)
{
	_putchar(ar);
	count += 1;
	return (count);
}
